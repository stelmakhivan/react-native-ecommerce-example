import {StyleSheet} from 'react-native';

import {COLORS} from 'constants/colors';

const styles = StyleSheet.create({
  row: {
    padding: 16,
    backgroundColor: COLORS.lightGray,
  },
  title: {textTransform: 'uppercase', fontWeight: '600'},
});

export default styles;
